{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "streamlit run Home.py --server.headless true --server.port $PORT --server.address 0.0.0.0 --server.enableCORS false --server.enableXsrfProtection false",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}
